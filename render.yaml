services:
  - type: web
    name: codearena-api
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: npm run start --workspace server
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_EXPIRES_IN
        value: 7d
